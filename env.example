# Tradier API (switch to live when ready)
TRADIER_ACCOUNT_ID=VA70062258
TRADIER_ACCESS_TOKEN=your-tradier-sandbox-token

# JWT secret for token validation
JWT_SECRET=your-very-secret-key

# Optional: change FastAPI port or other env vars if needed